import SwiftUI

struct IntroWindowView: View {
    var body: some View {
        VStack {
            Text("Let's Outdo Cancer")
                .font(.extraLargeTitle)
                .fontWeight(.bold)
                .padding()
        }
    }
}

#Preview {
    IntroWindowView()
}
